# MSInsight MCP 架构优化与演进需求文档 (PRD)

## 1. 概述
在完成从“传统透传代理”向“渐进式披露元工具网关 (Progressive Disclosure Meta-Tool Gateway)”的架构升级后，系统成功解决了大规模 API 直接暴露导致的 LLM 上下文爆炸与频繁幻觉问题。
然而，随着诊断剧本（Playbook）数量的增加及深度的下钻，系统在状态管理、上下文流转、配置复用及跨平台基建方面仍存在技术债务。本文档旨在梳理后续架构演进的核心痛点及解决方案，指导后续的开发与重构工作。

---

## 2. 核心架构编排演进

### 2.1 防范剧本规模化后的“上下文爆炸” (DAG 视界控制)
*   **痛点**：若未来的排查树演变成含数十个节点的复杂有向无环图 (DAG)，`search_profiler_tools` 若一次性全量下发整个剧本的 JSON Schema，依然会导致 LLM Context 溢出与幻觉。
*   **需求方案**：
    1.  **卡视野机制 (State-Aware Delivery)**：`search` 接口需具备状态感知能力。当查询命中剧本时，只返回【剧本摘要】与【第一层可用步骤】的 Tool Schema；随着排查深入，按需按层级动态暴露下游节点工具。
    2.  **击鼓传花 (Hints 接力)**：深度排查阶段，依托 `format_with_hints` 在响应末尾动态拼接【诊断法则】与【下一步推荐工具的 Schema】，引导 LLM 直接通过 `execute` 推进，减少无效的 `search` 轮次。
    3.  **剧本嵌套与子编排 (Sub-Playbooks)**：支持大剧本调用小剧本，当发现根因偏移（如通信慢变为发现内存泄漏），在 Hints 中引导 Agent 切换至内存诊断剧本。

### 2.2 公共前置逻辑复用 (Common Prerequisites Routing)
*   **痛点**：每一个场景剧本的 Step 1 几乎都需要执行 `import_trace_file`。如果在几十个 YAML 中重复编写，冗余度极高；且当 LLM 切换排查场景时，可能会因为剧本设定而重复请求加载分析文件。
*   **需求方案**：**（全局拦截网关 + YAML 继承机制组合）**
    1.  **基建级全局拦截**：在元工具网关入口判断 Session 的文件加载状态。若未加载，无视 LLM 搜什么高阶工具，强制阻断并返回要求调用 `import_trace_file` 初始化环境的提示。
    2.  **YAML 继承机制 (Mixins)**：抽象 `base_init.yaml` 等公共基础剧本，业务剧本支持 `extends: "base_init"`，由服务端在解析合并内存时构建完整 DAG，保持人工配置文件精简。

---

## 3. 会话控制与数据流转优化

### 3.1 跨步骤参数隐式流转 (Implicit Context Blackboard)
*   **痛点**：现有机制强依赖大模型阅读当前步骤的结果后，再“人工提取”特征（如某张卡的 `rankId`）作为参数喂给下一步。这一过程极易因大模型幻觉导致参数写错、数据类型不匹配等问题。
*   **需求方案**：
    引入 **“上下文黑板 (Context Board)”**。上游工具在 Python 层面执行完毕后，自动向黑板注册关键上下文（如 `slow_rank_list`）。下游工具被调用时，若大模型未传递/错传部分必须参数，网关优先从黑板中提取默认值自动补全填充，最大程度减少 LLM 对底层参数组装的参与。

### 3.2 会话状态污染防范 (Session State Pollution / Lifecycle)
*   **痛点**：状态机记录了已执行的步骤（`executed_tools`）和缓存（如 `current_kernel` 详情）。当用户在一次较长的对话中要求分析“迭代 A”，而后推翻要求重新分析“迭代 B”时，由于旧 Session 状态未清理，会导致鉴权逻辑被骗、查出脏数据。
*   **需求方案**：
    提供显式的 **“会话重置 / 任务重置”** 接口或内置逻辑。当分析流结束或探测到最外层的重置命令时，彻底清空 Session 内的 Cache 与执行拓扑集，保障多轮排查的干净纯粹。

### 3.3 剧本表达能力扩充 (Loop Expression)
*   **痛点**：目前基于 YAML 定义的排查 SOP 大多呈线性链路，缺乏应对“批量”异常节点的循环表达能力。例如某轮通信异常导致 3 张卡都是慢卡，若只有线性编排，大模型易遗漏其它异常网卡。
*   **需求方案**：
    支持排查节点的分支与循环描述（类似 `forEach` 或 `Map-Reduce` 语义）。在当前过渡期，可由业务工具主动通过 Hints 引导 LLM 做遍历操作：“发现 3 张慢卡，请对 Rank 4, 5, 6 循环调用下钻指令。”

### 3.4 动态传参盲区强校验 (Execute Endpoint Validation)
*   **痛点**：因网关收口为唯一的 `execute_profiler_tool(name, arguments: dict)` 工具，使得 LLM 客户端丧失了对具体业务工具结构原生 Schema Validation 的防错能力。
*   **需求方案**：
    利用 Pydantic 等库实现强壮入参验证中枢。如果大模型传入的 `arguments` `dict` 短缺必选键值，Python 后端应当快速拦截并返回人类可读、清晰的字段缺失报错（不要抛崩溃堆栈），直接告诉 LLM 缺了什么字段。

---

## 4. 工程基建与跨平台适配

### 4.1 跨进程自愈机制抽象
*   **痛点**：当前解决 C++ 僵尸进程重连使用的手段为 `taskkill /F /IM`，这使得整个 Python MCP 强力绑定到 Windows 系统栈。倘若向 Linux 容器环境进行部署和扩展，该机制将直接宕机并导致死锁。
*   **需求方案**：引入 `psutil` 等跨平台的进程生命周期管理库。

### 4.2 环境依赖与编译阻断修复
*   **痛点**：Agent 当前运行时常存在诸如 `ModuleNotFoundError: No module named 'loguru'` 的依赖错误，直接导致 `main.py` 无端报错推出，基础运行包的原子性不佳。
*   **需求方案**：规范 `requirements.txt` 的管理，在入口处对必要的三方包加载采取兼容性保障，提高生产稳定性。

---

## 5. 落地规划与优先级

此演进划分为三个核心阶段实施：
*   **P0 (当前急需修复)**：**清理会话状态污染 (3.2)** 与 **实现公共前置全局拦截 (2.2)**。这决定了系统真正跑通“多问多答”业务闭环的生命线。
*   **P1 (使用体验与稳定性补齐)**：**参数隐式黑板传递 (3.1)** 与 **执行传参强校验 (3.4)**。尽可能将犯错机会从大模型端收缴到 Python 服务端。
*   **P2 (进阶智能与基建加固)**：**YAML 树形继承 (2.2 & 2.1 补充)**、**循环表达支持 (3.3)** 与 **跨进程抽象自愈 (4.1/4.2)**。迈向大型复杂集群分布式监控方案的架构保障。
